#!/bin/sh

[ "$#" -ne 1 ] && echo "Only 1 argument required!" && exit 1

FILE="$BINDIR/$1"

if [ ! -f "$FILE" ]; then
	touch "$FILE"
	echo "#!/bin/sh" > "$FILE"
	chmod +x "$FILE"
fi

"$EDITOR" "$FILE"
